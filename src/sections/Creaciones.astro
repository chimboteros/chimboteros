---
import { timelineBrands } from '@/data/timelineBrands'
import { brands } from '@/data/brands'
---

<section id='creaciones' class='py-20 lg:py-32 px-6 lg:px-0'>
  <div class='max-w-[45rem] mx-auto text-center text-[#333] mb-10 lg:mb-20'>
    <h2 class='text-3xl lg:text-7xl mb-2 lg:mb-5 font-semibold'>
      Creaciones Culturales
    </h2>
    <p class='text-base lg:text-2xl'>
      Desarrollamos proyectos creativos en cultura, producción y turismo; que
      fortalecen nuestra identidad y educan en un estilo de vida más sustentable
      con el planeta.
    </p>
  </div>

  <div class='mx-auto w-10/12'>
    <ul class='relative grid gap-8 lg:gap-0 grid-cols-2 lg:grid-cols-10'>
      {
        timelineBrands.map(({ name, field, year }, index) => (
          <li
            class={`flex flex-col gap-4 text-center items-center justify-center`}
          >
            <img src={`/images/brands/${name}.png`} alt={`marca ${name}`} />

            <div>
              <h3 class='capitalize text-2xl font-medium'>{name}</h3>
              <i class='text-[#626971] text-xl'>{field}</i>
            </div>

            <i
              class={`hidden w-3 h-3 rounded-full border mt-7 mb-3 lg:flex items-center flex-shrink-0 ${
                year
                  ? 'border-[#F6034B] bg-[#F6034B]'
                  : 'border-[#333] bg-white'
              }`}
            >
              {index === 0 && (
                <span class='hidden lg:block absolute h-[1px] bg-[#333] w-[90%] -z-10' />
              )}
            </i>

            <p class='hidden lg:block text-base lg:text-2xl text-[#333]'>
              {year ?? '...'}
            </p>
          </li>
        ))
      }
    </ul>
  </div>

  <div class='flex flex-col gap-12 lg:gap-28 mt-28'>
    {
      brands.map(({ year, name, slogan, description, link, classes }) => (
        <div
          class={`${classes.container} relative h-[90vh] w-full lg:w-11/12 mx-auto bg-no-repeat bg-cover bg-center`}
        >
          <div
            class={`${classes.content} text-center pt-16 lg:pt-20 w-8/12 lg:w-[40rem] mx-auto`}
          >
            {year && (
              <small
                class={`${classes.small} text-base lg:text-xl border rounded-full py-1 px-4`}
              >
                {year}
              </small>
            )}

            <div class='my-3 lg:mb-5 capitalize'>
              <h3 class='text-5xl lg:text-6xl font-medium mb-1'>{name}</h3>
              <i class='text-2xl lg:text-3xl'>{slogan}</i>
            </div>

            <div class='text-base lg:text-2xl'>{description}</div>

            <div class={`${classes.cta} text-base lg:text-xl mt-3`}>
              {link ? (
                <a href={link} target='_blank' rel='noreferrer noopener'>
                  Ver más +
                </a>
              ) : (
                <p>:: Próximamente ::</p>
              )}
            </div>
          </div>
        </div>
      ))
    }
  </div>
</section>
