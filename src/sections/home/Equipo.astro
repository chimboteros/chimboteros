---
import { teamMembers } from '@/data/team'
import { collaborators } from '@/data/collaborators'
---

<section id='equipo' class='py-20 lg:py-32 px-6 lg:px-0'>
  <div class='max-w-[50rem] mx-auto text-[#1A1A1A] text-center mb-12 lg:mb-20'>
    <h2 class='text-3xl lg:text-7xl mb-2 lg:mb-5 font-semibold'>
      Equipo Creativo
    </h2>
    <p class='text-base lg:text-2xl lg:leading-10'>
      Un buen trabajo es posible gracias al talento, compromiso y valores
      compartidos de amigos chimboteros con una visión común: el cambio
      cultural.
    </p>
  </div>

  <ul class='w-full lg:w-10/12 mx-auto grid lg:grid-cols-5 gap-x-6 gap-y-11'>
    {
      teamMembers.map(({ name, field, summary }) => (
        <li class='text-center lg:text-left'>
          <img
            src={`/images/team/${name}.png`}
            alt={name}
            class='mb-4 lg:mb-5'
          />
          <h3 class='text-[#CD9632] text-2xl capitalize font-medium'>{name}</h3>
          <em class='text-lg text-[#a9a9a9]'>{field}</em>
          <p class='text-[#333] mt-0.5'>{summary}</p>
        </li>
      ))
    }
  </ul>

  <div
    class='w-full lg:w-10/12 mx-auto border-t border-b border-[#bbb] py-6 mt-12 text-center lg:text-left'
  >
    <h4 class='text-[#CD9632] text-2xl font-medium mb-5'>Colaboradores:</h4>

    <div class='flex flex-col lg:flex-row'>
      <ul class='flex-1 grid lg:grid-cols-4 gap-y-2'>
        {
          collaborators.map((collaborator) => (
            <li class='text-[#333]'>{collaborator}</li>
          ))
        }
      </ul>

      <div class='mt-5 lg:mt-0 lg:pr-24'>
        <em class='block text-[#7E7E7E]'>Súmate a los chimboteros</em>
        <a
          href='https://bit.ly/3xXu4Dh'
          target='_blank'
          rel='noreferrer noopener'
          class='text-[#FE0066] font-medium'>Postula aquí +</a
        >
      </div>
    </div>
  </div>

  <img
    src='/images/oficina-chimboteros.jpg'
    alt='miembros del equipo chimboteros charlando'
    class='hidden lg:block mt-28'
  />
  <img
    src='/images/mobile/silla.jpg'
    alt='silla chimbotera'
    class='lg:hidden mt-16 w-full'
  />
</section>
